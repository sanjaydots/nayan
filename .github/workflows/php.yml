name: Deploy to Cloud Run from Source
on:
  push:
    branches: [ "master" ]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: demo workflow job3
        uses: actions/checkout@v3
      - name: demo workflow job
        run: echo This is a demo start workflow
